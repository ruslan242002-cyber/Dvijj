'use strict';

/**
 * МИРОВЫЕ БОССЫ — общая HP-пуля на группу (3-5 человек), спавн раз в
 * 30-120 часов (реальный тайминг задаёт вызывающий код при спавне, не
 * здесь — здесь только сами боссы). Пока нет присланных боссов с
 * сопротивлениями — один плейсхолдер-босс для проверки всей системы
 * целиком, чтобы не откладывать инфраструктуру до получения реальных
 * данных. Формат намеренно совместим с уже готовой системой типов
 * урона (engine/damage-types.js) — resistances можно добавить любому
 * боссу без изменений в движке.
 */
const BOSSES = {
  test_colossus: {
    id: 'test_colossus',
    name: 'Тестовый Колосс',
    lore: 'Заглушка для проверки системы групповых боссов — реальные боссы придут с полными данными (сопротивления, лут, точный баланс).',
    hpPool: 15000,          // общая HP-пуля на группу — рассчитана так, чтобы 1 игрок не мог соло затащить в разумное время
    minParticipants: 3,     // ниже этого числа босс не спавнится/не начинает атаковать всерьёз
    maxParticipants: 5,
    stats: { power: 60, mind: 60, reaction: 40, endurance: 80, firepower: 90, shielding: 40 },
    luck: 20, accuracy: 0.85, dodge: 0.12, focus: 0.75,
    resistances: {}, // пусто — заполнится реальными данными, когда пришлёте (fire/poison/kinetic/emp/psionic)
    respawnMinHours: 30,
    respawnMaxHours: 120,
    reward: { credits: 5000, xp: 3000, resourcePool: [{ resource: 'Изотопы', tier: 5, qty: 20 }, { resource: 'Сплавы', tier: 5, qty: 20 }] },
    // ── Режим отряда (5 игроков онлайн одновременно, bosses/raid-engine.js) ──
    raidHp: 25000,       // отдельный пул HP для отрядного режима — не тот же hpPool, что у одиночного/асинхронного режима, битвы разные по духу
    raidAoeCadence: 3,    // раз в 3 раунда — АУЕ по всем, остальное время — по переду
    raidAoeDamagePct: 0.08, // АУЕ бьёт по % от максимума HP каждой цели, не фиксированным числом — одинаково опасно на любом уровне отряда
  },
};

function findBoss(bossId) {
  return BOSSES[bossId] || null;
}

module.exports = { BOSSES, findBoss };
